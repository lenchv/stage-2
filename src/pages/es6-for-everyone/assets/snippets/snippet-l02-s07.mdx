```js
const regexp = /ab+c/;
const regexp = new RegExp('ab+c')

const date = new Date()
date.getFullYear()

JSON.parse(text)
JSON.stringify(value)

Number.MAX_VALUE
Number.parseInt()

Math.sin(90)
Math.max(...values)
```