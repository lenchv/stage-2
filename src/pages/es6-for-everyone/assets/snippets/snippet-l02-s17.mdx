```js
const promise = new Promise((resolve, reject) => {
  // some asynchronous code
  let isValid = true 
	
  if (isValid) {  
    resolve("Success");  
  } else {  
    reject(Error("Error"));  
  }  
});
```